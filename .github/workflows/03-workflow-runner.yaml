name: 03-workflow-runner
on: 
    - pull_request
    - workflow_dispatch
      
jobs:
    show-runner:
      runs-on: windows-latest
      steps:
        - name: Window Runner
          shell: bash
          run: runner " ${{runner.os}} "
        
