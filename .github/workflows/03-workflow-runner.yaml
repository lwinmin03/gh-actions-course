name: 03-workflow-runner
on: 
    - pull_request
    - workflow_dispatch
      
jobs:
    show-runner:
      runs-on: windows-latest
      steps:
        - name: Show the runner
          run: runner " $RUNNER_OS "
